package com.guedes.tharseo.repositories;

import com.guedes.tharseo.models.TransactionSpotGrid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransactionSpotGridRepository extends JpaRepository<TransactionSpotGrid, Long> {
}
